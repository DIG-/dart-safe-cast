name: safe_cast
description: Dart library to safe cast
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
