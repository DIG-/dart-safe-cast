name: safe_cast
description: Dart library to provides safe way to cast objects or dynamics
version: 1.0.0
repository: https://github.com/DIG-/dart-safe-cast
issue_tracker: https://github.com/DIG-/dart-safe-cast/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
